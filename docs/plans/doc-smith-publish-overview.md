# doc-smith-publish: 一键发布 DocSmith 文档到线上

## 一句话说明

将 doc-smith-create 生成的 HTML 文档发布到 DocSmith Cloud，返回在线预览 URL。

## 为什么需要？

doc-smith-create 生成的文档存储在本地 `.aigne/doc-smith/dist/`，用户需要一种简单的方式将文档发布到线上预览和分享。之前的 publish 方案依赖大量 Node.js 脚本且与 MyVibe 强耦合，已被移除。新方案基于 myvibe-publish 裁剪改造，专注文档发布场景。

## 核心体验

```
用户: /doc-smith-publish

  ① 自动查找 .aigne/doc-smith/dist 目录
  ② 读取 config.yaml 获取 title / description
  ③ 授权（首次弹浏览器，后续自动）
  ④ ZIP 压缩 → 上传 → 转换 → 发布
  ⑤ 展示预览 URL + 写入 config.yaml

输出: 🔗 https://docsmith.aigne.io/user/z2qaXXX
```

## 架构

```
skills/doc-smith-publish/
├── SKILL.md                 ← Skill 入口（工作流编排）
└── scripts/
    ├── publish.mjs          ← 发布主逻辑（从 myvibe-publish 裁剪）
    └── utils/
        ├── auth.mjs         ← 授权（~/.aigne/docsmith-connected.yaml）
        ├── store.mjs        ← 凭证存储
        ├── history.mjs      ← 发布历史（workspace 内）
        ├── upload.mjs       ← TUS 文件上传
        ├── http.mjs         ← HTTP 封装
        ├── blocklet-info.mjs← API 路径解析
        ├── constants.mjs    ← 常量
        └── zip.mjs          ← 目录压缩
```

## 关键决策

| 问题 | 选择 | 原因 |
|------|------|------|
| 输入方式 | 仅 `--dir`，默认 dist/ | 文档场景固定，不需要 file/url |
| Metadata | 自动读取 config.yaml | 零配置体验 |
| 发布确认 | 跳过 | 信息已知且可重复发布 |
| 截图/标签 | 移除 | 文档发布不需要 |
| 凭证存储 | `~/.aigne/` | 与 .myvibe 分离 |
| 发布历史 | workspace 内 cache/ | 跟随项目，不污染全局 |
| 默认 Hub | docsmith.aigne.io | 专用文档托管 |

## 范围

**包含**：dist 目录发布、自动 metadata、授权、版本更新、vibeUrl 持久化

**不包含**：截图、标签、URL 导入、单文件发布、构建检测、确认流程

## 风险与缓解

| 风险 | 缓解 |
|------|------|
| Blocklet DID 兼容性 | docsmith.aigne.io 底层是同一套 MyVibe 系统 |
| 依赖版本 | 复用 myvibe-publish 验证过的版本 |

## 下一步

1. 从 myvibe-publish 复制 scripts/ 并裁剪改造
2. 编写 SKILL.md 工作流
3. 更新 doc-smith-create 完成提示（指向 `/doc-smith-publish`）
